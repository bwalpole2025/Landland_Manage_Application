{
  "name": "@landland/integrations",
  "version": "0.1.0",
  "private": true,
  "description": "External-integration adapters. StoragePort (object storage) with a Supabase Storage adapter and an in-memory adapter for tests. Keys are namespaced by owner so Storage RLS isolates by leading path segment.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {}
}
